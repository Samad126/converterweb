/** Tailwind v4 is a PostCSS plugin; there is no tailwind.config.js any more — the theme lives in `app/globals.css`. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
